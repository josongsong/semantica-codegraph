API                         MCP Tool                  Engine Layer                          Purpose
/search/code                code.search               retriever.intent + multi_index        자연어/심볼 기반 전체 코드 검색 (LLM 검색 엔트리)
/symbol/definition          symbol.definition         index.symbol + graph                  특정 심볼 정의(go-to-def) 위치 조회
/symbol/references          symbol.references         index.symbol + graph                  특정 심볼 참조(find-refs) 전체 탐색
/graph/neighbors            graph.neighbors           graph.query                           CALLS/IMPORTS 등 인접 관계 탐색(GraphRAG 기본)
/file/open                  file.open                 git_provider + chunk_store            파일 또는 특정 라인 범위 코드 읽기

/repomap/overview           repomap.overview          repomap.builder + summarizer          레포 구조 및 주요 노드 요약 제공 (Context Builder)
/callchain/trace            callchain.trace           graph.query(CALLS)                    함수 호출 체인 forward/backward 트레이싱
/route/lookup               route.lookup              domain_meta + graph                   HTTP route → handler 역매핑(API 분석)
/dto/usage                  dto.usage                 graph + index.symbol                  DTO 정의 + 사용 지점 전체 분석
/indexing/status            indexing.status           snapshot_store                        인덱싱 상태/진행도/마지막 커밋 조회

/ir/dump                    ir.dump                   ir.store or build_on_fly              IRDocument 디버깅 및 AST→IR 확인
/graph/dump                 graph.dump                graph.store                           특정 노드의 부분 그래프 dump (debug)
/index/debug/search         index.debug_search        index.lexical + index.vector          BM25/embedding/fusion 스코어 디버깅
/profiling/scenario         profiling.search_scenarios profiling.store                      검색 품질/속도/정확도 프로파일링
