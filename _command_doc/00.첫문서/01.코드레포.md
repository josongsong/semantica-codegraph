root/
  client/
    mui-cli/
    web-cli/

  server/                     # 외부 인터페이스 전용 (HTTP/MCP)
    api/                      # REST / gRPC / Websocket
    mcp/                      # Claude · Serena MCP layer
    adapters/                 # 요청 → src.engine 포트로 변환

  src/                        # Code Engine 전체 (핵심)
    foundation/               # Parsing → AST → IR → Graph → Chunk
      parsing/
      ir/
      graph/
      chunk/

    repomap/                  # Chunk 이후: 프로젝트 지도 + 중요도 트리
      builder/
      pagerank/
      summarizer/
      tree/

    index/                    # Static Index Family
      lexical/
      vector/
      symbol/
      fuzzy/
      domain_meta/
      runtime/

    retriever/                # Query → Multi-index → Graph Expand → Fusion
      intent/
      multi_index/
      graph_runtime_expansion/
      fusion/
      context_builder/

    engine.py                 # 파이프라인 엔트리포인트
    ports.py                  # server 계층이 호출하는 Port 인터페이스
