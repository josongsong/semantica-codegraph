root/
  client/
    mui-cli/
    web-cli/

  server/
    api/
    mcp/
    adapters/

  src/
    foundation/
      parsing/
      ir/                     # Structural IR (Node/Edge/Type/Signature/CFG 스냅샷) :contentReference[oaicite:0]{index=0}
        model.py
        builder.py

      semantic_ir/            # Semantic IR 계층 (Type/Signature/CFG/DFG/BFG 통합) :contentReference[oaicite:1]{index=1}
        context.py            # Snapshot/Delta/SemanticIndex

        typing_ir/
          model.py
          resolver.py
          builder.py

        signature_ir/
          model.py
          builder.py

        cfg_ir/
          model.py
          builder.py

        bfg_ir/               # 신규: Branch Flow Graph (브랜치 구조 전용)
          model.py            # BranchBlock, BranchEdge, BranchRegion 등
          builder.py          # CFG → BFG 변환 (함수 단위)
          projector.py        # BFG → GraphDocument 투영 훅(옵션, graph/projection 으로 옮겨도 됨)

        dfg_ir/
          model.py            # VariableEntity, ReadWriteEvent, DataFlowEdge 등 :contentReference[oaicite:2]{index=2}
          analyzer.py
          builder.py
          resolver.py

      graph/
        projection/
          semantic_projection.py  # Semantic IR(Type/Signature/CFG/DFG/BFG) → GraphNode/GraphEdge
                                  # - CFGBlock → CFG_* edges
                                  # - DFG → DATA_FLOW / READS / WRITES
                                  # - BFG → BRANCH_FLOW / REGION edges

      chunk/
        base_chunker.py
        code_chunker.py
        incremental.py

    repomap/
      builder/
      pagerank/
      summarizer/
      tree/

    index/
      lexical/
      vector/
      symbol/
      fuzzy/
      domain_meta/
      runtime/

    retriever/
      intent/
      multi_index/
      graph_runtime_expansion/
      fusion/
      context_builder/

    engine.py
    ports.py

  container.py
  config.py
