# Nextest SOTA Configuration - 최대 속도 최적화
# Docs: https://nexte.st/book/configuration.html

[profile.default]
retries = 0
fail-fast = true
slow-timeout = { period = "10s", terminate-after = 1 }  # 10초 초과시 즉시 중단
test-threads = "num-cpus"
status-level = "skip"  # 출력 최소화

[profile.default.junit]
path = "target/nextest/junit.xml"

# 🚀 TDD용: 초고속 (5초 timeout)
[profile.tdd]
retries = 0
fail-fast = true
slow-timeout = { period = "5s", terminate-after = 1 }
test-threads = "num-cpus"
status-level = "fail"
default-filter = "not test(/stress|benchmark|perf|slow|e2e|integration/)"

# ✅ 개발용: 기본 테스트만
[profile.fast]
retries = 0
fail-fast = true
slow-timeout = { period = "15s", terminate-after = 2 }
test-threads = "num-cpus"
default-filter = "not test(/stress|benchmark|perf|slow/)"

# 🔗 통합 테스트
[profile.integration]
retries = 1
fail-fast = false
slow-timeout = { period = "60s", terminate-after = 2 }
default-filter = "test(/integration|e2e/)"

# 🐢 CI용 (전체)
[profile.ci]
retries = 2
fail-fast = false
slow-timeout = { period = "120s", terminate-after = 3 }
