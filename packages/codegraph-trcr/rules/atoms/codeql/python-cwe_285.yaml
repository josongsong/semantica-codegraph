# TRCR Rules - Auto-generated

atoms:
- id: sink.cwe_285.PamAuthorization
  kind: sink
  tags: ['cwe_285', 'PamAuthorization', 'vulnerability']
  severity: critical
  cwe: ["CWE-285"]
  description: Not using `pam_acct_mgmt` after `pam_authenticate` to check the validity of a login can lead to auth
  match:
  -     call: PamAuthorization

- id: sink.cwe_285.PamAuthorization
  kind: sink
  tags: ['cwe_285', 'PamAuthorization', 'vulnerability']
  severity: critical
  cwe: ["CWE-285"]
  description: Not using `pam_acct_mgmt` after `pam_authenticate` to check the validity of a login can lead to auth
  match:
  -     call: PamAuthorization