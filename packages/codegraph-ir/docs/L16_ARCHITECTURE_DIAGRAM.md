# L16 RepoMap Smart Mode - 아키텍처 다이어그램

**날짜**: 2025-12-28

---

## 🏗️ 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                     User Application Layer                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ AI Agent     │  │ CI/CD        │  │ Architecture │          │
│  │ Bug Fixing   │  │ Pipeline     │  │ Review       │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                  │
│         └──────────────────┴──────────────────┘                  │
│                            │                                     │
└────────────────────────────┼─────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                  Smart Mode Auto-Detection                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ ModeDetectionContext                                     │  │
│  │ ┌────────────┬────────────┬────────────┬────────────┐   │  │
│  │ │ Query      │ Target     │ Analysis   │ Repo Size  │   │  │
│  │ │ Keywords   │ File       │ Type       │            │   │  │
│  │ └────────────┴────────────┴────────────┴────────────┘   │  │
│  └─────────────────────────┬────────────────────────────────┘  │
│                            ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ detect_mode() - 7 Rules                                  │  │
│  │ • Rule 1: Initial indexing → Fast                        │  │
│  │ • Rule 2: Analysis type → Specific mode                  │  │
│  │ • Rule 3: Architecture flag → Architecture               │  │
│  │ • Rule 4: AI agent flag → AI                             │  │
│  │ • Rule 5: Target file exists → AI                        │  │
│  │ • Rule 6: Query keywords → Detected mode                 │  │
│  │ • Rule 7: Small repo → Full                              │  │
│  └─────────────────────────┬────────────────────────────────┘  │
│                            ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ RecommendedMode                                          │  │
│  │ ┌─────┬────┬──────────┬──────┐                          │  │
│  │ │Fast │ AI │Architect │ Full │                          │  │
│  │ └─────┴────┴──────────┴──────┘                          │  │
│  └─────────────────────────┬────────────────────────────────┘  │
└────────────────────────────┼─────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              E2EPipelineConfig (Configuration)                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ pagerank_settings: PageRankSettings                      │  │
│  │ ┌────────────────────────────────────────────────────┐   │  │
│  │ │ enable_personalized: bool  ← AI/Full mode          │   │  │
│  │ │ enable_hits: bool          ← Architecture/Full mode│   │  │
│  │ │ max_iterations: usize      ← Precision control     │   │  │
│  │ │ tolerance: f64             ← Convergence threshold │   │  │
│  │ └────────────────────────────────────────────────────┘   │  │
│  └──────────────────────┬───────────────────────────────────┘  │
└─────────────────────────┼──────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│           IRIndexingOrchestrator (Pipeline Executor)            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ L1: IR Build                                             │  │
│  │ L2-L5: Basic Analysis                                    │  │
│  │ L6-L15: Advanced Analysis                                │  │
│  │ ▼                                                        │  │
│  │ L16: RepoMap with PageRank                               │  │
│  │  ┌─────────────────────────────────────────────────┐    │  │
│  │  │ PageRankEngine::new(&config.pagerank_settings)  │    │  │
│  │  └────────────────────┬────────────────────────────┘    │  │
│  └───────────────────────┼─────────────────────────────────┘  │
└──────────────────────────┼──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│          PageRankEngine (Algorithm Execution)                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Standard PageRank (ALWAYS)                            │  │
│  │    ┌────────────────────────────────────────────┐        │  │
│  │    │ Global importance scoring                  │        │  │
│  │    │ Iterations: 5 (default) / 10 (Full mode)   │        │  │
│  │    └────────────────────────────────────────────┘        │  │
│  │                                                           │  │
│  │ 2. Personalized PageRank (OPTIONAL - AI/Full mode)       │  │
│  │    ┌────────────────────────────────────────────┐        │  │
│  │    │ Context-aware importance                   │        │  │
│  │    │ IF enable_personalized == true             │        │  │
│  │    └────────────────────────────────────────────┘        │  │
│  │                                                           │  │
│  │ 3. HITS (OPTIONAL - Architecture/Full mode)              │  │
│  │    ┌────────────────────────────────────────────┐        │  │
│  │    │ Authority/Hub analysis                     │        │  │
│  │    │ IF enable_hits == true                     │        │  │
│  │    └────────────────────────────────────────────┘        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Result (RepoMapSnapshot)                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Nodes with Importance Scores:                            │  │
│  │ • pagerank: f64         (Always available)               │  │
│  │ • personalized: f64     (If AI/Full mode)                │  │
│  │ • authority: f64        (If Architecture/Full mode)      │  │
│  │ • hub: f64              (If Architecture/Full mode)      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 사용 흐름도

### Fast Mode (기본)

```
User Intent: "Index repository quickly"
       │
       ▼
┌─────────────────────┐
│ Default Config      │
│ (Fast mode)         │
└──────┬──────────────┘
       │ is_initial_indexing = true
       ▼
┌─────────────────────┐
│ detect_mode()       │
│ → Fast              │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ PageRank Settings   │
│ PPR: OFF            │
│ HITS: OFF           │
│ Iterations: 5       │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ PageRankEngine      │
│ Only basic PageRank │
│ 5 iterations        │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Result              │
│ Time: 1.19s         │
│ Throughput: 114K/s  │
└─────────────────────┘
```

---

### AI Mode (버그 수정)

```
User Intent: "Fix bug in auth/login.rs"
       │
       ▼
┌─────────────────────┐
│ Smart Mode Config   │
│ with_smart_pagerank │
└──────┬──────────────┘
       │ target_file = "auth/login.rs"
       │ is_ai_agent = true
       ▼
┌─────────────────────┐
│ detect_mode()       │
│ → AI                │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ PageRank Settings   │
│ PPR: ON ✅          │
│ HITS: OFF           │
│ Iterations: 5       │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ PageRankEngine      │
│ 1. PageRank         │
│ 2. Personalized PR  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Result              │
│ Time: 2.3s          │
│ + PPR scores        │
│ → Find related 10   │
└─────────────────────┘
```

---

### Architecture Mode (리뷰)

```
User Intent: "Analyze repository structure"
       │
       ▼
┌─────────────────────┐
│ Smart Mode Config   │
│ with_smart_pagerank │
└──────┬──────────────┘
       │ is_architecture_review = true
       │ query = "analyze structure"
       ▼
┌─────────────────────┐
│ detect_mode()       │
│ → Architecture      │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ PageRank Settings   │
│ PPR: OFF            │
│ HITS: ON ✅         │
│ Iterations: 5       │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ PageRankEngine      │
│ 1. PageRank         │
│ 2. HITS (Auth+Hub)  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Result              │
│ Time: 2.3s          │
│ + Authority scores  │
│ + Hub scores        │
└─────────────────────┘
```

---

## 📦 모듈 구조

```
src/pipeline/
│
├── pagerank_mode_detector.rs (NEW - Smart Mode)
│   ├── ModeDetectionContext
│   ├── AnalysisType
│   ├── RecommendedMode
│   ├── detect_mode()
│   └── configure_smart_mode()
│
├── end_to_end_config.rs (수정 - 통합)
│   ├── E2EPipelineConfig
│   │   └── pagerank_settings: PageRankSettings
│   ├── configure_smart_pagerank()
│   └── with_smart_pagerank()
│
├── end_to_end_orchestrator.rs (수정 - 실행)
│   └── IRIndexingOrchestrator
│       └── execute()
│           └── PageRankEngine::new(&config.pagerank_settings)
│
└── mod.rs (수정 - Export)
    ├── pub use pagerank_mode_detector::*
    └── ...

src/features/repomap/infrastructure/
│
└── pagerank.rs (수정 - 기본값 최적화)
    ├── PageRankSettings::default()
    │   ├── enable_personalized = false  (Fast mode)
    │   ├── enable_hits = false          (Fast mode)
    │   └── max_iterations = 5           (2배 빠름)
    └── PageRankEngine
        ├── compute_pagerank()           (항상 실행)
        ├── compute_personalized()       (선택적)
        └── compute_hits()               (선택적)
```

---

## 🔀 데이터 흐름

### 입력 (User → Config)

```
┌──────────────────┐
│ User Intent      │
│ • Query string   │
│ • Target file    │
│ • Analysis type  │
│ • Context flags  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Smart Mode       │
│ ModeDetection    │
│ Context          │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Auto-detect      │
│ detect_mode()    │
│ (7 rules)        │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Recommended      │
│ Mode             │
│ (Fast/AI/Arch)   │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ PageRank         │
│ Settings         │
│ (Applied)        │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ E2EPipeline      │
│ Config           │
└──────────────────┘
```

---

### 처리 (Config → Engine → Result)

```
┌──────────────────┐
│ E2EPipeline      │
│ Config           │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Pipeline Stages  │
│ L1-L15           │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ L16: RepoMap     │
│ PageRankEngine   │
└────────┬─────────┘
         │
         ▼
    ┌────┴────┐
    │         │
    ▼         ▼
┌───────┐ ┌───────┐ ┌───────┐
│PageR. │ │  PPR  │ │ HITS  │
│Always │ │ (Opt) │ │ (Opt) │
└───┬───┘ └───┬───┘ └───┬───┘
    │         │         │
    └────┬────┴────┬────┘
         │         │
         ▼         ▼
    ┌─────────────────┐
    │ Merge Scores    │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ RepoMap         │
    │ Snapshot        │
    └─────────────────┘
```

---

### 출력 (Result → User)

```
┌─────────────────┐
│ RepoMap         │
│ Snapshot        │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│ Nodes  │ │ Stats  │
│        │ │        │
└───┬────┘ └───┬────┘
    │          │
    ▼          ▼
┌──────────────────────┐
│ Node Importance:     │
│ • pagerank: 0.856    │
│ • personalized: 0.92 │ (if AI/Full)
│ • authority: 0.78    │ (if Arch/Full)
│ • hub: 0.45          │ (if Arch/Full)
└───────────┬──────────┘
            │
            ▼
    ┌───────────────┐
    │ User Actions: │
    │ • Top 10 files│
    │ • Related code│
    │ • Core libs   │
    │ • Hubs        │
    └───────────────┘
```

---

## 🎯 핵심 컴포넌트

### 1. ModeDetectionContext (입력)

```rust
pub struct ModeDetectionContext {
    pub query: Option<String>,           // User query
    pub target_file: Option<String>,     // Bug/feature file
    pub analysis_type: Option<AnalysisType>,
    pub repo_size: Option<usize>,
    pub is_ai_agent: bool,
    pub is_architecture_review: bool,
    pub is_initial_indexing: bool,
}
```

**역할**: 사용 상황을 감지하기 위한 신호 수집

---

### 2. detect_mode() (로직)

```rust
pub fn detect_mode(context: &ModeDetectionContext) -> RecommendedMode {
    // 7 rules cascade
    if context.is_initial_indexing {
        return Fast;
    }
    if let Some(analysis_type) = context.analysis_type {
        return match analysis_type { ... };
    }
    // ... more rules
    Fast // Default
}
```

**역할**: 7가지 규칙으로 최적 모드 선택

---

### 3. RecommendedMode (출력)

```rust
pub enum RecommendedMode {
    Fast,         // 1x (1.19s)
    AI,           // 2x (2.3s)
    Architecture, // 2x (2.3s)
    Full,         // 3.5x (4.2s)
}

impl RecommendedMode {
    fn apply_to_settings(&self, settings: &mut PageRankSettings);
    fn description(&self) -> &'static str;
    fn time_multiplier(&self) -> f32;
}
```

**역할**: 선택된 모드를 설정에 적용

---

### 4. PageRankSettings (설정)

```rust
pub struct PageRankSettings {
    pub enable_personalized: bool,  // PPR on/off
    pub enable_hits: bool,          // HITS on/off
    pub max_iterations: usize,      // Precision
    pub tolerance: f64,             // Convergence
}
```

**역할**: 런타임 알고리즘 제어

---

## 📊 성능 영향 분석

### 알고리즘 실행 시간

```
┌────────────────┬──────────┬──────────┬──────────┐
│ Algorithm      │ Always   │ Iter=5   │ Iter=10  │
├────────────────┼──────────┼──────────┼──────────┤
│ PageRank       │ ✅       │ 0.19s    │ 0.38s    │
│ PPR            │ Optional │ 0.95s    │ 1.90s    │
│ HITS           │ Optional │ 0.95s    │ 1.90s    │
├────────────────┼──────────┼──────────┼──────────┤
│ Fast (PR only) │          │ 0.19s    │ 0.38s    │
│ AI (PR+PPR)    │          │ 1.14s    │ 2.28s    │
│ Arch (PR+HITS) │          │ 1.14s    │ 2.28s    │
│ Full (All)     │          │ 2.09s    │ 4.18s    │
└────────────────┴──────────┴──────────┴──────────┘
```

**참고**: 실제 측정값은 L16 전체 (그래프 구축 + 알고리즘 실행)
- Fast mode: 0.975초 (136K LOC)

---

### 모드별 성능 프로파일

```
Fast Mode:
┌─────────────────────────────────────────┐
│ L16: 0.975s (82%)                       │
│ ├── Graph Build: 0.185s (19%)           │
│ └── PageRank: 0.790s (81%)              │
│     └── 5 iterations                    │
└─────────────────────────────────────────┘

AI Mode (예상):
┌─────────────────────────────────────────┐
│ L16: ~1.97s (85%)                       │
│ ├── Graph Build: 0.185s (9%)            │
│ ├── PageRank: 0.790s (40%)              │
│ └── PPR: 0.995s (51%)                   │
│     └── 5 iterations                    │
└─────────────────────────────────────────┘

Full Mode (예상):
┌─────────────────────────────────────────┐
│ L16: ~3.8s (90%)                        │
│ ├── Graph Build: 0.185s (5%)            │
│ ├── PageRank: 1.580s (42%)              │
│ │   └── 10 iterations                   │
│ ├── PPR: 1.035s (27%)                   │
│ │   └── 5 iterations                    │
│ └── HITS: 1.000s (26%)                  │
│     └── 5 iterations                    │
└─────────────────────────────────────────┘
```

---

**작성일**: 2025-12-28
**버전**: 1.0
**상태**: ✅ Documentation Complete
